// JavaScript removeAttribute() Method;
const googleLink = document.getElementById("js");
console.log(googleLink);

if (googleLink) {
  let removeAttribute = googleLink.removeAttribute("target");
  console.log(removeAttribute);
}
